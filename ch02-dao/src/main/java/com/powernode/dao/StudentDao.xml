<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.powernode.dao.StudentDao">
    <!--    <select id="selectBlog" resultType="Blog">-->
    <!--        select * from Blog where id = #{id}-->
    <!--    </select>-->

    <!--
    查询一个学生student
    <student> : 表示查询操作，里面是select语句
    id:要执行的sql语句的唯一标识，是一个自定义字符串，
       推荐使用dao接口中的方法名称
    resultType：告诉mybatis，执行sql语句，把数据赋值给哪个类型的java对象。
                resultType的值现在使用的是java对象的全限定名称

-->
    <select id="seleceById" resultType="com.powernode.domain.Student">
        select id, name, email, age
        from student
        where id = #{studentid}
    </select>

    <!--
    insert into student values(1001,"666","333",100);
    如果传入给mybatis是一个java对象，使用#{属性名}获取此属性的值。
    属性值放到#{}占位符的位置，mybatis执行此属性 对应的getxxx()，
    例如#{id}，执行getid();
    -->

</mapper>

        <!--
    1.约束文件
    http://mybatis.org/dtd/mybatis-3-mapper.dtd
    约束文件作用：定义和限制当前文件中可以使用的标签和属性，以及标签出现的顺序。

    2.mapper是根标签
    namespace：  命名空间，必须有值，不能为空，唯一值。
                推荐使用Dao接口的全限定名称。
         作用:    参与识别sql语句的作用

-->